# Day 4 Report

## Technical Summary
Today I learned Infrastructure as Code (IaC). I created a shell script to automate server setup and used YAML to define infrastructure configuration. I also understood how GitHub Actions can automate tasks on every push.

## Bug Log
Error: YAML file was not working properly due to incorrect indentation.  
Solution: I fixed the spacing and made sure to use spaces instead of tabs.

## Conceptual Reflection
Using code instead of manual setup is important because it saves time, reduces errors, and allows us to scale easily. If we have to set up 100 servers, writing code once is much more efficient than doing it manually every time.